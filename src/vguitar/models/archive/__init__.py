"""Frozen neural / system-ID era (2025-2026): CIRCE3, TCN, RNN, SSM, Volterra,
Wiener-Hammerstein, their losses, the benchmark harness, and the Duffing testbed.

Archived when the project pivoted to the physical DK-method emulator — see
``outputs/knowledge/findings.json`` for the consolidated verdicts and
``docs/archive/sota-campaign.md`` for the full decision log. Importable on demand
(requires the ``neural`` extra: ``uv sync --extra neural``); never auto-registered.
"""
