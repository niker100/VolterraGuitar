# --- Python ---
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ty_cache/

# --- Project artifacts (comments on their own line: gitignore has no inline comments) ---
# generated SPICE datasets (large; reproducible from code)
data/
# plots, audio renders, archived raw results (local) -- EXCEPT the consolidated
# knowledge store, which is the tracked machine-readable record of the
# benchmarking era
outputs/*
!outputs/knowledge
# training runs / checkpoints
runs/
*.pth
*.ckpt
*.onnx

# --- v1 / Micro-Cap generated junk (kept circuit*.cir as reference) ---
*.inx*
*.TNO
*.ANO
*.DANO
*.DDNO
*.CSV
*.bak_cir
stored.pckl
volterra_nn_model.h5
*.h5

# --- Claude Code local state (runtime lock, session scratch) ---
.claude/
